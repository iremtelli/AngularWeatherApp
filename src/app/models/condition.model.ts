export class Condition {
  text: string;
  icon: string;
  code: number;


  constructor(text: string, icon: string, code: number) {
    this.text = text;
    this.icon = icon;
    this.code = code;
  }
}
